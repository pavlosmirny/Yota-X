"use client";

import styles from "./SectionDivider.module.css";

const SectionDivider: React.FC = () => {
  return <div className={styles.container}>{/* SVG градиенты */}</div>;
};

export default SectionDivider;
